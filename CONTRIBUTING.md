# Contributing

Thanks for helping improve Cinematheca.

## Local Setup

```sh
npm install
make install-hooks
make test
make smoke
```

Use Conventional Commits for commit messages, for example `feat: add subtitle export preset`.

## Pull Requests

Keep changes focused, include tests for behavior changes, and run `make lint`, `make test`, and `make smoke` before pushing.
